<% if logged_in? %>
    <script>
        window.currentUser = {
          'id': <%= current_user.id %>,
          'email': '<%= current_user.email %>',
          'first_name': '<%= current_user.first_name %>',
          'last_name': '<%= current_user.last_name %>'
        }
    </script>
<% end %>

<div id="root">
    <h1>React</h1>
</div>